# Local SQLite (works without installing Postgres)
DATABASE_URL="file:../data/cms.db"

# Production: switch prisma/schema.prisma provider to postgresql
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/pdfflow?sslmode=require"

NEXT_PUBLIC_SITE_NAME="PDFFlow"
NEXT_PUBLIC_SITE_TAGLINE="Simple PDF tools for every device."
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_CONTACT_EMAIL="techpreneur3@gmail.com"
NEXT_PUBLIC_WHATSAPP="+250 786328597"

# Required in production
# ADMIN_PASSWORD=

# Required on Vercel so live admin can save featured images and guides.
# Set these in the Vercel project environment variables. Never commit the token.
# BLOB_STORE_ID=
# BLOB_READ_WRITE_TOKEN=
